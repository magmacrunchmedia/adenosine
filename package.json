{
  "name": "adenosine",
  "version": "0.2.0",
  "private": true,
  "description": "A collection of web game engines by MagmaCrunch",
  "license": "Apache-2.0",
  "repository": "https://github.com/magmacrunchmedia/adenosine",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check": "node scripts/check-packaging.mjs && node scripts/check-no-hardcoded-hosts.mjs"
  }
}
